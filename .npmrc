# @vercel/analytics declares SvelteKit as an optional peer, which npm tries to
# resolve and conflicts with Vitest's Vite 5 even though this app doesn't use
# Svelte. Legacy peer resolution skips that phantom conflict. Vercel honors this.
legacy-peer-deps=true
